<form method="post" action="{{ path('highconcept_delete', {'id': highconcept.id, 'idProjet': projet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ highconcept.id) }}">
    <button class="btn btn-danger">Supprimer le highconcept</button>
</form>
